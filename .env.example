# Runtime configuration. Never put credentials into browser code or Git.
# Set through Sites environment variables after observing the owner's Site user ID.
PORTFOLIO_OWNER_ID=
# Optional cap for total reserved + stored media; default 20 GiB.
PORTFOLIO_STORAGE_LIMIT_BYTES=21474836480
